//handles a field where data can be imported via url
// routes to appropriate file type handler
// csv, api, etc
// try to route based on response headers? how realistic is that?
// 

"use strict";
define(["./csv", "./datamap"], function(csvParser, dataMapUI) {
    return function() {

    };
});
