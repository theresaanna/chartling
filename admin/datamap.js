// renders ui and packages data for visualization export
// based on user choices
// maps content verticals (csv to json keys, etc) to
// fields in the visualization through a UI
// that is probably drag and drop

"use strict";
define(function() {
    return function() {

    };
});
